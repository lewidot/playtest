# Playtest

An example automation testing project in Python using Playwright and Pytest. The website under test is [Sauce Demo](https://www.saucedemo.com).

Run tests with the following command:
```sh
python -m pytest
```